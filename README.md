# REFRESHYO CHROME EXTENSION

A chrome extension that helps user refresh a particular URL automatically at a specified interval. It takes in URL and Interval as a "job" and refreshes the Tab at the interval specified in the job. A job can be created, can paused and can be deleted.
